# Salin file ini menjadi .env lalu isi nilai sebenarnya.
# .env TIDAK boleh di-commit ke Git (sudah ada di .gitignore).

# --- Database ---
DB_HOST=localhost
DB_PORT=3306
DB_NAME=padi_pjok
DB_USER=padi_user
DB_PASS=ganti_dengan_password_kuat

# --- Aplikasi ---
APP_TZ=Asia/Jakarta
APP_URL=https://padipjok.pintarhub.com
APP_ENV=production
